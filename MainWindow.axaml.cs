using Avalonia.Controls;

namespace EchoView;

public partial class MainWindow : Window
{
    // The constructor body for this partial class lives in Program.cs.
    // MainWindow.axaml.cs only calls InitializeComponent(); the Init() method
    // (defined in Program.cs) is invoked from there to wire up the UI.
}
